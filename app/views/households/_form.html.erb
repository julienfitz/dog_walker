<div class="well col-sm-5 col-xs-12">

  <%= form_for([@user, @household]) do |f| %>
  <% if @household.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@household.errors.count, "error") %> prohibited this household from being saved:</h2>

      <ul>
      <% @household.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h5><%= f.label :owner_name %><br>
    <%= f.text_field :owner_name %></h5>
  </div>
  <div class="field">
    <h5><%= f.label :address %><br>
    <%= f.text_field :address %></h5>
  </div>
  <div class="field">
    <h5><%= f.label :phone %><br>
    <%= f.text_field :phone, id: "inputPhone", :class => "phone_us" %>
  </div>
  <div class="field">
    <h5><%= f.label :email %><br>
    <%= f.text_field :email %></h5>
  </div>
  <div class="field">
    <h5><%= f.label :key_info %><br>
    <%= f.text_field :key_info %></h5>
  </div>

  <div class="actions">
    <%= f.submit "Update", class: "btn btn-primary" %>
  </div>

<% end %>

<%= link_to 'Back', :back %>

</div>

