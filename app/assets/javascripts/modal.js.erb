$(function(){
  var appt_id,
      pet_name,
      div_id,
      id;

  $(".appointment").on('click', '#modal-button', function(event){
  // $("#modal-button").click(function(event){
    event.preventDefault();
    appt_id = $('#appt').val($(this).data("id"));
    pet_name = $('#text').val($(this).data("value"));
    div_id = "#"+$(this).data("id");
    id = $(this).data("id");
    console.log(id);
    $('#myModal').modal('toggle');
  });

  $(".table").on('click', '#delete-appt', function(){
    id = $(this).data("id");
    div_id = "#"+$(this).data("id");
    $(div_id).remove();
    $.ajax({
      url:'/appointments/'+id, 
      type: 'DELETE'
    });
  });

  $("#send-text").click(function(){
    $(div_id).remove();
    $('#myModal').modal('toggle');
  });

  $("#cancel-appt").click(function(){
    $(div_id).remove();
    $('#myModal').modal('toggle');
  });

  $("#no-text").click(function(){
    $(div_id).remove();
    $('#myModal').modal('toggle');
    $.ajax({
      url:'/appointments/'+id, 
      type: 'DELETE'
    });
  });

  $(".contact-walker").on('click', '#email-modal-button', function(event){
    event.preventDefault();
    var walker_id = $('#walker').val($(this).data("id"));
    var walker_name = $('#text').val($(this).data("value"));
    div_id = "#"+$(this).data("id");
    id = $(this).data("id");
    console.log(id);
    $('#myModal').modal('toggle');
  });

  $("#send-email").click(function(){ 
    $('#myModal').modal('toggle');
  });
});


